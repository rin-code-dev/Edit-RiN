# 画面端・横画面設定の修正

- ダークモードの画面内に残っていた黒→surface色の縦グラデーションを単色の背景黒に変更。ナビゲーションバーと横画面のノッチ余白との色差を解消する修正です。ライトとMaterial Youのグラデーションは維持します。実機での確認は別途必要です。

- メイン画面と設定画面のシステムバー・ノッチ余白をScaffoldに集約。内側で余白を消費済みにしてからIME余白を適用し、余分な二重計算を避けます。
- ステータスバーを隠しても、縦画面ではdisplayCutoutの余白を維持します。
- ウィンドウ自体はノッチ領域まで描画可能にし、Compose側で余白を制御します。
- 設定「外観」に「横画面でノッチ部分まで使用」を追加。初期値はオフ（ノッチを避ける）。オンでは横画面のメイン・設定画面のノッチ余白を除きます。カメラ穴に重なる内容が隠れる点は設定内にも表示します。設定は端末内とZIPバックアップに保存します。
- システムバー領域の背景は従来のアプリ背景色に戻しました（ダークでは黒）。灰色の帯は使用しません。操作パネルの追加の下余白を8dpから2dpに削減する変更は維持。OSのナビゲーション操作領域は確保し、ボタンを重ねません。
- 横画面の設定は左側のカテゴリ切替＋右側の選択カテゴリのスクロール表示に変更。左右の余白・メニュー幅を縮め、メインと同じ細い枠線・角丸を使用します。縦画面は全カテゴリ表示を維持します。
- 作品比率や編集中プレビュー、全画面プレビューの仕様は変更しません。

ノッチ形状、ジェスチャー／3ボタンナビゲーション、ソフトキーボードによる違いは実機での追加確認が必要です。
